# MediaPipe Volume Changer
Small test project to test MediaPipe, which lets you adjust the system volume with a simple hand gesture.

## Run
1. Python 3.11.x
2. Create and activate a virtual enviorement
3. Install dependencies: pip install opencv-python mediapipe pyautogui.
4. Run: python main.py
